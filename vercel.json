{
  "buildCommand": "pnpm install --frozen-lockfile && ../../node_modules/.bin/expo export --platform web --output-dir dist",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "framework": null
}
