{
  "buildCommand": "pnpm install --frozen-lockfile && export VERCEL_DIST=$(pwd)/dist && pnpm --filter @workspace/garden-table run build:web",
  "outputDirectory": "dist",
  "installCommand": "echo skip",
  "framework": null
}
